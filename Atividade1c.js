const prompt = require("prompt-sync")();

   let numeroA = Number(prompt("Digite um numero: "))
   let numeroB = Number(prompt("Digite outro numero: "))

   console.log("A soma deles é, ", numeroA + numeroB);